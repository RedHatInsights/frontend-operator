---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: guestbooks.webapp.test.k8s.elastic.co
spec:
  group: webapp.test.k8s.elastic.co
  names:
    kind: Guestbook
    listKind: GuestbookList
    plural: guestbooks
    singular: guestbook
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Guestbook is the Schema for the guestbooks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            default:
              page: 1
            description: GuestbookSpec defines the desired state of Guestbook.
            properties:
              certificateRef:
                description: CertificateRef is a reference to a secret containing
                  a certificate
                properties:
                  group:
                    default: ""
                    description: |-
                      Group is the group of the referent. For example, "gateway.networking.k8s.io".
                      When unspecified or empty string, core API group is inferred.
                    maxLength: 253
                    pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    default: Secret
                    description: Kind is kind of the referent. For example "Secret".
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the referent.
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the backend. When unspecified, the local
                      namespace is inferred.


                      Note that when a namespace different than the local namespace is specified,
                      a ReferenceGrant object is required in the referent namespace to allow that
                      namespace's owner to accept the reference. See the ReferenceGrant
                      documentation for details.


                      Support: Core
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              entries:
                description: Entries contain guest book entries for the page
                items:
                  description: GuestbookEntry defines an entry in a guest book.
                  properties:
                    comment:
                      description: |-
                        Comment by guest. This can be a multi-line comment.
                        Like this one.
                        Now let's test a list:
                        * a
                        * b


                        Another isolated comment.


                        Looks good?
                      pattern: 0*[a-z0-9]*[a-z]*[0-9]*|\s
                      type: string
                    name:
                      description: Name of the guest (pipe | should be escaped)
                      maxLength: 80
                      pattern: 0*[a-z0-9]*[a-z]*[0-9]
                      type: string
                    rating:
                      description: Rating provided by the guest
                      maximum: 5
                      minimum: 1
                      type: integer
                    time:
                      description: Time of entry
                      format: date-time
                      type: string
                  type: object
                type: array
              enum:
                description: Enumeration is an example of an aliased enumeration type
                enum:
                - MyFirstValue
                - MySecondValue
                type: string
              headers:
                description: Headers contains a list of header items to include in
                  the page
                items:
                  description: GuestbookHeaders are strings to include at the top
                    of a page.
                  type: string
                maxItems: 10
                type: array
                uniqueItems: true
              page:
                default: 1
                description: Page indicates the page number
                example: 3
                minimum: 1
                type: integer
              selector:
                description: Selector selects something
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              str:
                type: string
            required:
            - certificateRef
            - enum
            - str
            type: object
            x-kubernetes-validations:
            - message: Please start a new book.
              rule: self.page < 200
          status:
            description: GuestbookStatus defines the observed state of Guestbook.
            properties:
              status:
                allOf:
                - enum:
                  - OK
                  - Unknown
                  - Error
                - enum:
                  - OK
                  - Error
                type: string
              str:
                type: string
            required:
            - status
            - str
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
