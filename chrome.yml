apiVersion: cloud.redhat.com/v1alpha1
kind: Frontend
metadata:
  name: chrome
spec:
  # Add fields here
  envName: env-boot
  title: Chrome
  deploymentRepo: http://test
  API:
    versions:
      - v1
  frontend:
    paths:
      - /
      - /config/chrome
  # image: quay.io/redhat-cloud-services/chrome:ci-beta
  image: quay.io/bholifie/chrome:v0.0.4
